# 設定ファイルをリロードする
bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded!"

set-option -g prefix C-t
bind -r k select-pane -U 
bind -r j select-pane -D 
bind -r h select-pane -L 
bind -r l select-pane -R

# C-bのキーバインドを解除
unbind C-b

# ステータスバーをトップに配置する
set-option -g status-position top

# 左右のステータスバーの長さを決定する 
set-option -g status-left-length 90
set-option -g status-right-length 90

# #P => ペイン番号
# 最左に表示
set-option -g status-left '#[fg=colour255,bg=colour54,bold] 🦄 #S #[fg=colour54,bg=colour235,nobold]#[fg=colour141,bg=colour235] #(whoami) #[fg=colour235,bg=colour234]'

# Wi-Fi、バッテリー残量、現在時刻
# 最右に表示
set-option -g status-right '#[fg=colour235,bg=colour234]#[fg=colour141,bg=colour235] %H:%M #[fg=colour54,bg=colour235]#[fg=colour255,bg=colour54,bold] %m/%d '

# ステータスバーを10秒毎に描画し直す
set-option -g status-interval 10

# センタライズ（主にウィンドウ番号など）
set-option -g status-justify centre

# vimのキーバインドでペインをリサイズする
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# | でペインを縦分割する
bind | split-window -h

# - でペインを縦分割する
bind - split-window -v

# 番号基準値を変更
set-option -g base-index 1
set-window-option -g pane-base-index 1

# ウィンドウを閉じた時に番号を詰める
set-option -g renumber-windows on

# 新しいウィンドウ/ペインを現在のディレクトリで開く
bind c new-window -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# ペイン間の移動をより簡単に（prefix不要）
bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

# ウィンドウ切り替え（Shift+矢印キーで）
bind -n S-Left previous-window
bind -n S-Right next-window

# アクティブペインをより分かりやすく
set -g pane-border-status top
set -g pane-border-format "#[fg=colour141,bold] 💜 #{pane_index} #{pane_current_command}"


#### basic settings

set-option -g status-justify "left"
set-window-option -g mode-keys vi
# set-option -g status-fg purple
# set-option -g status-bg black
set -g pane-active-border-style fg=colour166,bg=default
# set -g window-style fg=colour10,bg=default
# set -g window-active-style fg=colour12,bg=default
set-option -g history-limit 64096

set -sg escape-time 10

#### COLOUR

# default statusbar colors (紫ベース)
set-option -g status-style bg=colour234,fg=colour141,default

# default window title colors
set-window-option -g window-status-style fg=colour141,bg=colour234,dim

# active window title colors (紫で強調)
set-window-option -g window-status-current-style fg=colour255,bg=colour141,bold

# pane border (紫のアクセント)
set-option -g pane-border-style fg=colour239
set-option -g pane-active-border-style fg=colour141,bg=default,bold

# message text (紫のアクセント)
set-option -g message-style bg=colour54,fg=colour255,bold

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green

# allow the title bar to adapt to whatever host you connect to
set -g set-titles on
set -g set-titles-string "#T"


# マウス操作を有効にする
set-option -g mouse on
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"

# マウス選択でクリップボードにコピー
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel "pbcopy"

# コピーモードを設定する
# コピーモードでvimキーバインドを使う
setw -g mode-keys vi

# 'v' で選択を始める
bind -T copy-mode-vi v send -X begin-selection

# 'V' で行選択
bind -T copy-mode-vi V send -X select-line

# 'C-v' で矩形選択
bind -T copy-mode-vi C-v send -X rectangle-toggle

# 'y' でヤンク（macOSクリップボードにコピー）
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy"

# 'Y' で行ヤンク（macOSクリップボードにコピー）
bind -T copy-mode-vi Y send -X copy-pipe-and-cancel "pbcopy"

# 'C-p'でペースト
bind-key C-p paste-buffer

set -g default-shell /bin/zsh

# import
if-shell "uname -s | grep -q Darwin" "source ~/.config/tmux/tmux.conf.osx"

# Powerline (コメントアウト - powerline-configが無い場合)
source ~/.config/tmux/tmux.powerline.conf

# シンプルな時刻表示を追加 (tmux-powerlineを使う場合はコメントアウト)
# set-option -g status-right '#[fg=colour166,bold][%Y-%m-%d %H:%M] #[fg=colour135]#h'

# TPM plugins (tmux-powerlineは無効化)
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'erikw/tmux-powerline'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# run '~/.config/tmux/plugins/tpm/tpm'

